package de.pierreschwang.spigotlib.nms.implementation;

/**
 * Defines methods and other stuff in classes inside the net.minecraft.server package.
 * These methods may change in every new version, so the implementation must be made specifically for every version.
 */
public interface Implementation {
}
